# Protocol

This directory is reserved for shared schema artifacts that are independent of
the Rust mobile boundary. The active typed mobile API is handwritten in
`shared/rust-bridge/codex-mobile-client`, and UniFFI outputs are generated under
`shared/rust-bridge/generated/`.
